# Base URL for search_*.db / detail_*.db files.
# Production: the R2 public bucket URL, e.g. https://pub-xxxx.r2.dev
# Leave unset for local dev (serves from static/data via a range-capable server).
VITE_DATA_BASE_URL=
